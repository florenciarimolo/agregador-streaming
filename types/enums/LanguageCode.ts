/**
 * ISO 639-1 + ISO 3166-1 alpha-2 locale codes (language-REGION)
 * Used for TMDB API and content preferences
 */
export enum LanguageCode {
  SPANISH = 'es-ES',
  CATALAN = 'ca-ES',
  BASQUE = 'eu-ES',
  GALICIAN = 'gl-ES',
  ENGLISH = 'en-US',
  ENGLISH_UK = 'en-GB',
}

